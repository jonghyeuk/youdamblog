{
  "name": "semifab-scientific-ai-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "SemiFab DSE Orchestra interactive demo",
  "scripts": {
    "build": "node -e \"console.log('SemiFab static demo ready')\"",
    "serve": "firebase serve --only hosting",
    "start": "firebase serve --only hosting",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "devDependencies": { "firebase-tools": "^13.0.0" },
  "license": "MIT"
}
